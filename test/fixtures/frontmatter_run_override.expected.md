---
markdown-run:
  defaults:
    run: false
---

```ruby run=true
puts "Should execute despite global run=false"
```

```ruby RESULT
puts "Should execute despite global run=false"
# >> Should execute despite global run=false
```
